# SN-Utils-slash-commands

## /rows [number]
Run this command from a list to see any number of rows.  Very useful when needing to take action on large number of rows using a UI action or another slash command like /copycells

